



//const alpha = ["red","green","yellow"]
    //alpha.forEach((color)=>console.log(color))
    
   // const length = alpha.map((color)=>color.length)
   // console.log(lengths)
 //console.log(length)
 const beta= [9,10,11]
 beta.forEach((x)=>console.log(x+8));

 const gamma = beta.map((x)=>x*2)
 console.log(gamma)